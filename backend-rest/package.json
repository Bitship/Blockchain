{
  "name": "bitship-backend-rest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "composer-rest-server --card admin@bitship --namespace always --port 3000 --authentication false --multiuser false --websockets true --tls false",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "composer-rest-server": "^0.19.1"
  }
}
